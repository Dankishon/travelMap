# Быстрый старт TravelMap Russia

## 🚀 Запуск через Docker Compose

```bash
cd infra
docker-compose up -d
```

Подождите ~30 секунд для инициализации БД и миграций.

### Доступ к сервисам:
- **Frontend**: http://localhost
- **API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs
- **PostgreSQL**: localhost:5432

## 📝 Что происходит при запуске:

1. PostgreSQL запускается и создаётся БД `travelmap`
2. API контейнер:
   - Выполняет миграции Alembic (`alembic upgrade head`)
   - Инициализирует места (`python init_places.py`)
   - Запускает FastAPI сервер
3. Frontend собирается и запускается через nginx

## 🔧 Локальная разработка

### Backend

```bash
cd apps/api

# Установка зависимостей
pip install -r requirements.txt

# Настройка .env (скопируйте из .env.example)
# DATABASE_URL=postgresql://travelmap:travelmap123@localhost:5432/travelmap

# Миграции
alembic upgrade head

# Инициализация данных
python init_places.py

# Запуск сервера
uvicorn app.main:app --reload
```

### Frontend

```bash
cd apps/web

# Установка зависимостей
npm install

# Создайте .env.local:
# VITE_API_URL=http://localhost:8000/api/v1

# Запуск dev сервера
npm run dev
```

## 🧪 Тестирование

### Backend тесты
```bash
cd apps/api
pytest
```

### Frontend тесты
```bash
cd apps/web
npm test
```

## 🔍 Проверка работы

1. Откройте http://localhost
2. Зарегистрируйте пользователя
3. Войдите в систему
4. На карте выберите места и добавьте их в маршрут
5. Перейдите в "Мои маршруты" для просмотра сохранённых маршрутов

## 📚 Дополнительная документация

- `README.md` - Полная документация проекта
- `STRUCTURE.md` - Описание структуры кода

