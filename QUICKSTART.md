# Быстрый старт TravelMap Russia v2.0

## Вариант 1: Запуск через Docker (рекомендуется) 🐳

### Шаг 0: Запустите Docker Desktop

**Важно!** Перед запуском убедитесь, что Docker Desktop запущен:
- Откройте приложение **Docker Desktop** на вашем Mac (Applications → Docker)
- Или через командную строку: `open -a Docker`
- Дождитесь полной загрузки (иконка Docker в строке меню должна быть зелёной)
- Проверьте: выполните `docker ps` - должна быть пустая таблица без ошибок

**Если видите ошибку "Cannot connect to the Docker daemon"** → см. файл `START_DOCKER.md`

### Шаг 1: Запуск

```bash
docker-compose up --build
```

### Шаг 2: Откройте в браузере

```
http://localhost:5001
```

**Примечание:** Используется порт 5001 вместо 5000, так как порт 5000 часто занят системным процессом macOS (AirPlay Receiver).

Готово! Приложение работает.

Для остановки нажмите `Ctrl+C` или выполните:
```bash
docker-compose down
```

---

## Вариант 2: Локальный запуск

### Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 2: Инициализация данных

```bash
python init_data.py
```

Это создаст папку `data/` и файл `places.json` с достопримечательностями.

### Шаг 3: Запуск сервера

```bash
python app.py
```

Сервер запустится на `http://localhost:5000`

### Шаг 4: Откройте в браузере

```
http://localhost:5000
```

Flask теперь раздаёт и статические файлы, отдельный веб-сервер не нужен.

## Шаг 5: Использование

1. Зарегистрируйте нового пользователя
2. Войдите в систему
3. Исследуйте карту и добавляйте места в маршрут
4. Ваш маршрут сохраняется на сервере!

## Проверка работы API

### Регистрация
```bash
curl -X POST http://localhost:5000/api/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"test123"}'
```

### Вход
```bash
curl -X POST http://localhost:5000/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"test123"}'
```

### Получить места (без авторизации)
```bash
curl http://localhost:5000/api/places
```

## Устранение проблем

**Ошибка "Module not found"**
- Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`

**Ошибка CORS в браузере**
- Убедитесь, что сервер запущен на порту 5000
- Проверьте, что Flask-CORS установлен

**Карта не загружается**
- Проверьте консоль браузера на ошибки
- Убедитесь, что API доступен: `curl http://localhost:5000/api/places`

